{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "setup": "yarn install && npx lerna bootstrap --npm-client=yarn",
    "start": "lerna run start:dev --npm-client=yarn",
    "build": "lerna run build --npm-client=yarn",
    "start:prod": "lerna run start:prod --npm-client=yarn"
  }
}
