{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "setup": "yarn install && npx lerna bootstrap --npm-client=yarn",
    "start": "lerna run start:dev --npm-client=yarn"
  }
}
